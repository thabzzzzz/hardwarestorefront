server {
  listen 80;
  server_name localhost;

  # Development: do not set CORS headers here to avoid duplicates.
  # Let the application (Laravel) set `Access-Control-Allow-*` headers.

  root /var/www/html/public;
  index index.php index.html;

  location / {
    try_files $uri $uri/ /index.php?$query_string;
  }

  # Handle CORS preflight: return 204; headers are set globally above with `always`
  if ($request_method = OPTIONS) {
    return 204;
  }

  location ~ \.php$ {
    include fastcgi_params;
    fastcgi_pass app:9000;
    fastcgi_index index.php;
    fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
  }

  access_log /var/log/nginx/access.log;
  error_log /var/log/nginx/error.log;
}

# Copy this file to `nginx/dev-default.conf` locally (ignored by git)
# and use the example compose override to mount it into the web container.
